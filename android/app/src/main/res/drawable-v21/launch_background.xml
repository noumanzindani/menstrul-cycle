<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- @color/splash_background has a values-night variant, so this follows
         the OS theme the way ?android:colorBackground used to, but lands on the
         LunarFlow ground instead of plain white/black. -->
    <item android:drawable="@color/splash_background" />
</layer-list>
