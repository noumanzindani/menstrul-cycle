<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Local notifications: post on Android 13+, reschedule after reboot.
         Uses INEXACT alarms, so no SCHEDULE_EXACT_ALARM permission is needed. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- App lock (local_auth) -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>

    <!-- Health Connect: READ basal/body temperature only. This is an ON-DEVICE
         permission — reads are local IPC to the Health Connect app, NOT network.

         This block used to claim that INTERNET is deliberately absent and that
         its absence was "the app's bright-line privacy guarantee". That stopped
         being true when accounts and Firestore sync landed: firebase_core,
         firebase_auth, cloud_firestore and google_mobile_ads all merge INTERNET
         in from their own library manifests, so the merged manifest has it and
         has had it for some time. Media makes the app's use of the network
         louder still. The claim is removed rather than softened — a comment
         that reads as a privacy guarantee must not be one the code disproves. -->
    <uses-permission android:name="android.permission.health.READ_BODY_TEMPERATURE"/>

    <!-- Media picking goes through the Android system Photo Picker
         (image_picker's pickMultipleMedia), which returns a per-item read grant
         and needs NO media permission. These removals are a regression guard,
         not a fix: as of the versions in pubspec.lock, image_picker_android and
         video_player_android declare no permissions at all — but a future
         update that added one would ship it silently via manifest merge, and
         Play SCANS THE MERGED MANIFEST.

         That matters because Google Play restricts READ_MEDIA_IMAGES /
         READ_MEDIA_VIDEO to apps whose CORE PURPOSE is photo or video, with a
         declaration form and a review. A period tracker does not qualify, so
         shipping one of these is a rejection, not a warning.

         ACCESS_MEDIA_LOCATION is removed for a different and sharper reason: it
         is what UN-REDACTS GPS coordinates in photos returned by the picker.
         Without it the platform strips location, which is the only thing
         standing between an intimate photo and the user's home address being
         uploaded to an operator-readable bucket. Never request it. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"
        tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"
        tools:node="remove"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:node="remove"/>
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION"
        tools:node="remove"/>

    <!-- Strip the advertising-ID permission that google_mobile_ads injects via
         manifest merge. The app ONLY ever requests NON-personalized ads
         (AdService.request = AdRequest(nonPersonalizedAds: true)), so it never
         uses the ad ID. Removing it keeps the Play Data Safety story honest and
         avoids the AD_ID declaration requirement. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove"/>

    <!-- allowBackup=false: the health DB must never leave the device via
         Android Auto Backup / cloud backup — that would break the app's core
         "your data can't leave your phone" promise. tools:replace guarantees
         this wins any library manifest merge that sets allowBackup="true". -->
    <application
        android:label="LunaTrack"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        tools:replace="android:allowBackup">

        <!-- flutter_local_notifications receivers -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false"/>
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Health Connect asks the app to show its privacy rationale here
                 (Android 13-). Required by the `health` plugin. -->
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE"/>
            </intent-filter>
        </activity>
        <!-- Health Connect permissions-usage screen (Android 14+). Points back
             at MainActivity to show the same rationale. -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".MainActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE"/>
                <category android:name="android.intent.category.HEALTH_PERMISSIONS"/>
            </intent-filter>
        </activity-alias>
        <!-- AdMob App ID. This is Google's OFFICIAL TEST app ID — replace with
             your real app ID from https://apps.admob.com before release. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713"/>

        <!-- Home-screen widget: "N days until your next period". Data is pushed
             from Dart (HomeWidgetService); this receiver only re-renders it.
             exported=true so the launcher can send APPWIDGET_UPDATE. -->
        <receiver
            android:name=".LunaWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/luna_widget_info"/>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Lets the app detect / deep-link to the Health Connect app. -->
        <package android:name="com.google.android.apps.healthdata"/>
    </queries>
</manifest>
